"use client";

import PlaceholderPage from "@/components/common/PlaceholderPage";
import { Tags } from "lucide-react";

/** 标签管理 */
export default function TagsPage() {
  return (
    <PlaceholderPage
      icon={Tags}
      title="标签管理"
      description="文章标签的创建、编辑、删除功能即将上线"
    />
  );
}

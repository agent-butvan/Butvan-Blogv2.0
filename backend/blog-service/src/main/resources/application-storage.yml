# ============================================================================
# 文件存储配置 — 支持本地存储 / MinIO 对象存储动态切换
# ============================================================================

storage:
  # 存储类型: local（本地磁盘）| minio（MinIO 对象存储）
  type: minio
  minio:
    endpoint: http://47.102.205.85:19000
    access-key: minio
    secret-key: wj08265395
    bucket: blog2
    # 是否为公共只读桶（默认为 true，若设为 false 则为私有桶，使用7天预签名URL访问）
    public-read: true
    # 自定义域名（可配置为 CDN 加速域名，留空则默认使用 endpoint 拼接）
    custom-domain: ${STORAGE_MINIO_CUSTOM_DOMAIN:}

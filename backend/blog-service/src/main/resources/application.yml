server:
  port: 8080

spring:
  application:
    name: blog-service
  
  config:
    import:
      - classpath:application-database.yml
      - classpath:application-routes.yml
      - classpath:application-redis.yml

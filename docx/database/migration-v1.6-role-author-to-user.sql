-- ============================================================================
-- 角色模型调整：移除 AUTHOR 角色，统一为 USER
-- 变更说明：普通用户注册后默认为 USER（原 AUTHOR），后台管理员为 ADMIN（站长）
-- 创建时间: 2026-07-09
-- ============================================================================

-- 将所有 AUTHOR 角色的用户统一更新为 USER
UPDATE blog_user
SET role = 'USER',
    updated_at = CURRENT_TIMESTAMP
WHERE role = 'AUTHOR';
